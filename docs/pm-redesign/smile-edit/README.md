# 웃지마게임 미리보기 편집

## 현재 미리보기 — 2026-09-14

준비 장면만 반복하던 영상을, 한 경기의 준비 → 웃음 감지 → 공격 성공·방어 실패 → 승리·패배 결과가 보이는 원본 녹화로 교체했다. `초대배틀.mp4`와 `랜덤매칭1.mp4`, `랜덤매칭2.mp4`를 비교해 결과 화면에 2점 대 1점이 표시되는 `랜덤매칭1.mp4`를 선택했다.

- 원본: `C:/Users/ljh43/OneDrive/Desktop/workspace/SSAFY/프로젝트/2. 공통pjt/캡처/랜덤매칭1.mp4`.
- 원본 SHA-256: `1d0bc67664a7725d46aed4a76fa1721de6d5a2895429bb0e8af779670b7e3ce4`.
- 사용 구간: 11.5–23.0초(매칭된 참가자·준비·시작), 35.0–41.5초(웃음 감지·공격 성공), 51.5–71.0초(방어 실패·다음 공격·2:1 결과).
- 결과물: `public/pm/previews/smile.mp4`, 1280×720, 16:9, 24fps, 37.5초, H.264, 무음, faststart, 1,299,128바이트.
- 포스터: 같은 원본의 37.6초. 두 참가자와 실제 ‘웃음 감지: 100%’ UI가 보인다. 이는 해당 순간의 화면 표시이며 모델 정확도 측정값이 아니다. WebP 32,856바이트.
- 편집: 한 경기 안에서 대기 구간 두 곳을 생략했다. 원본 속도를 유지하고, 바깥 여백과 오른쪽 스크롤바를 제외하도록 x=80, y=70에서 1120×630으로 크롭했다. 화면·점수·판정 문구·커서를 수정하거나 합성하지 않았다.
- 공개 설명: 팀 서비스의 랜덤 매칭 시연임을 표시했다. 본인의 설문·기획·초대방 구현과 팀원의 모델 개발·배틀 AI 연동 구분을 유지했다.

`edit-match-preview.py`에 원본 해시 검사, 컷 범위와 인코딩 설정을 기록했다. `--source`, `--ffmpeg`, `--output-dir`, `--manifest` 인자로 경로를 받는다. `match-preview.json`은 실제 실행 결과의 해시·용량·구간 기록이다. 이전 두 파일은 `.codex_tmp/smile-full-20260914/smile-before.mp4`와 `smile-before.webp`에 보관했다.

원본의 준비·웃음 감지·판정·결과 프레임과 최종 편집본을 대조했고, 전체 디코딩을 통과했다. 배포 대상과 같은 별도 빌드에서 20개 페이지 생성, 계약 테스트 74개, ESLint 경고 0건, 14개 페이지의 링크·미디어 대상 46개 검사를 통과했다. 브라우저에서 37.5초·1280×720 재생 정보를 확인하고 재생·일시정지를 조작했다. 메인과 상세를 1440px 및 390px 너비에서 확인했으며, 가로 넘침이나 캡션 잘림은 없었다.

## 이전 미리보기 — 2026-09-11

2026-09-11. 기존 13초 미리보기에서 상단 메뉴·방 코드·프로필·스크롤바·검은 여백·페이지 스크롤이 화면을 복잡하게 만들어, 준비와 게임 시작만 보여 주는 5.33초 영상으로 편집했다.

## 결과물

- `public/pm/previews/smile.mp4`: 960×720, 4:3, 24fps, 128프레임, H.264, 무음, faststart. 260,537바이트.
- `public/pm/previews/smile.webp`: 두 사람이 READY 상태인 포스터. 26,806바이트.
- 기존 미리보기 백업: `.codex_tmp/smile-edit/smile-before.mp4`, `smile-before.webp`.
- 원본: `C:/Users/ljh43/OneDrive/Desktop/workspace/SSAFY/프로젝트/2. 공통pjt/캡처/초대배틀.mp4`.
- 원본 SHA-256: `dd59c389ba982f86e66f975f9491bdeaac7bbdb04249082c41f462ced3db74b7`.

## 편집 내용

1. 원본 8.90~11.45초: 스크롤이 멈춘 뒤의 준비 화면을 사용. 두 참가자 카드와 READY 버튼을 중심으로 크롭했다. 메뉴, 방 코드, 스크롤바, 하단 경계와 넓은 여백이 빠진다. 버튼을 누르는 커서는 남겼다.
2. 원본 13.30~17.45초: 숫자와 BATTLE START만 확대했다. 아래의 TODAY'S TIP은 화면 밖으로 제외하고, 움직이는 커서는 프레임별 작은 영역을 보간해 지웠다.
3. 숫자 사이의 빈 구간과 과도하게 확대되는 순간을 제거했다. 24fps 기준 숫자별 8프레임을 1/2배속으로 재생해 3·2·1을 읽을 시간을 확보하고, 마지막 시작 문구는 원래 속도를 사용했다. 따라서 실제 대기시간을 그대로 보여 주는 녹화가 아닌 편집된 미리보기다.
4. 영상 처음 0.15초, 끝 0.25초에 짧은 페이드를 넣었다. 실제 플레이 및 참여자 얼굴이 나오는 구간은 사용하지 않았다.

원본 화면을 새로 그리거나 게임 결과를 생성하지 않았다. 기존 앱의 준비 상태 변화와 시작 애니메이션을 크롭·컷 편집·시간 조정했으며, 포트폴리오 설명도 편집본임을 밝힌다.

## 재현과 검증

`edit-smile.py`가 컷 범위, 크롭, 커서 탐지·제거, 리타이밍, 인코딩을 기록한다. `cursor-cleanup.json`에는 제거한 영역의 원본 좌표를 남겼다. 스크립트의 원본 경로와 FFmpeg 경로는 현재 로컬 환경 기준이며, 실행 결과는 `.codex_tmp/smile-edit/`에 만들어진다.

- 준비 → 두 사람 READY → 3·2·1 → BATTLE START 장면 검토.
- 영상 전체 디코딩 성공, 오디오 트랙 없음, 960×720·5.33초 확인.
- 프로덕션 빌드·TypeScript·수정한 PM 코드의 ESLint 통과.
- 브라우저에서 실제 재생·일시정지와 4:3 비율 확인. 390px 모바일에서도 가로 넘침과 영상 잘림 없음.
